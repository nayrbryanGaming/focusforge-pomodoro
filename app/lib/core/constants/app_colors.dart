import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:google_fonts/google_fonts.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFFFF6B35); // Deep Orange
  static const Color primaryLight = Color(0xFFFFA573);
  static const Color primaryDark = Color(0xFFC34A1B);

  // Secondary/Background
  static const Color background = Color(0xFF0F172A); // Dark Navy
  static const Color surface = Color(0xFF1E293B); // Slate
  static const Color surfaceHighlight = Color(0xFF334155);

  // Accent
  static const Color accent = Color(0xFFFFD166); // Soft Yellow

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
}
